#ifndef BINARIZATION_FILTER_HPP
#define BINARIZATION_FILTER_HPP

template <typename SourcePixelType, typename DestinationImageType,
        typename SourceImageType = char>
class BinarizationFilter {
};

#endif
